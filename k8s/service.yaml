apiVersion: v1
kind: Service
metadata:
  name: QRcode-destroyer-service
spec:
  selector:
    app: QRcode-destroyer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP